apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: ingress-{{ .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  rules:
    - host: {{ .Release.Name }}.{{ .Values.domain }}
      http:
        paths:
          - backend:
              serviceName: {{ .Release.Name }}-kb-http
              servicePort: 5601