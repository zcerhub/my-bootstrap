kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: networkpolicy-{{ .Release.Name }}
spec:
  podSelector:
    matchLabels:
      release: {{ .Release.Name }}
  ingress: 
  - from:
    - podSelector: {}
    - namespaceSelector: {}
          

          
